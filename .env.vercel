# Vercel Postgres Configuration
# Get from: Vercel Dashboard → Storage → Database → .env.local tab

# Vercel akan otomatis set ini setelah connect database:
# POSTGRES_URL=
# POSTGRES_PRISMA_URL=
# POSTGRES_URL_NON_POOLING=
# POSTGRES_USER=
# POSTGRES_HOST=
# POSTGRES_PASSWORD=
# POSTGRES_DATABASE=

# Manual set (required):
CONNECTOR_API_KEY=your-production-api-key-change-this
QUERY_TIMEOUT=30000
NODE_ENV=production

# Instructions:
# 1. Deploy project ke Vercel via dashboard
# 2. Create Vercel Postgres di Storage tab
# 3. Connect database to project
# 4. Vercel otomatis menambahkan POSTGRES_* variables
# 5. Tambahkan CONNECTOR_API_KEY via Settings → Environment Variables
# 6. Redeploy otomatis setelah save
